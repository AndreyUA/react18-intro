// import styled from "styled-components";

// Components
import Title from "../Title";
import Wrapper from "../Wrapper";

const useDeferredValue = () => {
  return (
    <Wrapper>
      <Title content="Usage of useDeferredValue hook." />
    </Wrapper>
  );
};

export default useDeferredValue;

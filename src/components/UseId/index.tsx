import styled from "styled-components";

// Styled
const Wrapper = styled.div`
  flex: 2 1 auto;
  height: 100%;
`;

const UseId = () => {
  return <Wrapper>UseId</Wrapper>;
};

export default UseId;
